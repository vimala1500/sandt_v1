# Environment Variables for Stock Backtesting Dashboard

# Data Fetching Configuration
# ============================

# Timeout for yfinance API requests (in seconds)
# Default: 30 seconds
# Recommended for Cloud Run: 60 seconds (to account for cold starts)
YFINANCE_TIMEOUT=60

# Maximum number of retry attempts for failed data fetches
# Default: 3
# Recommended: 3-5 (balances reliability vs. response time)
YFINANCE_MAX_RETRIES=3

# Application Configuration
# =========================

# Dash debug mode (set to False in production)
DASH_DEBUG=False

# Server port (Cloud Run uses PORT environment variable)
# PORT=8080

# Deployment Notes
# ================

# For Google Cloud Run deployment:
# --------------------------------
# Set these environment variables using:
#
# gcloud run services update stock-dashboard \
#   --set-env-vars YFINANCE_TIMEOUT=60,YFINANCE_MAX_RETRIES=3 \
#   --region us-central1

# For local development:
# ----------------------
# 1. Copy this file to .env (don't commit .env to git)
# 2. Load environment variables:
#    - Linux/Mac: export $(cat .env | xargs)
#    - Windows: Use set command for each variable
# 3. Or use python-dotenv package

# Network Requirements
# ====================

# The application requires internet access to:
# - query1.finance.yahoo.com (Yahoo Finance API)
# - query2.finance.yahoo.com (Yahoo Finance API backup)
# - fc.yahoo.com (Yahoo Finance cookie/session)
#
# Ensure Cloud Run has egress connectivity enabled:
# gcloud run services update stock-dashboard --vpc-egress all-traffic
